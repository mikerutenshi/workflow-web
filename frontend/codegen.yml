overwrite: true
schema: 'http://localhost:3001/graphql'
documents: 'app/api/queries/*.gql'
generates:
  app/api/generated/types.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
    config:
      useTypeImports: true
